# Development profile
server:
  port: 8085

# Bypass authentication in dev mode
security:
  bypass-auth: true

logging:
  level:
    com.davidparker.dms.llm: DEBUG
    org.springframework.security: DEBUG
