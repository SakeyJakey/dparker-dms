apiVersion: apps/v1
kind: Deployment
metadata:
  name: dms-api-gateway-service
  namespace: dms-dev
  labels:
    app: dms-api-gateway-service
    service: api-gateway
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dms-api-gateway-service
  template:
    metadata:
      labels:
        app: dms-api-gateway-service
        service: api-gateway
        version: v1
    spec:
      serviceAccountName: dms-api-gateway-service
      containers:
      - name: dms-api-gateway-service
        image: dms-api-gateway-service:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: PORT
          value: "8080"
        - name: SERVER_PORT
          value: "8080"
        - name: ENVIRONMENT
          value: dev
        - name: SPRING_PROFILES_ACTIVE
          value: dev
        - name: ADMIN_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: dms-service-urls-configmap
              key: admin.service
        - name: AUDIT_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: dms-service-urls-configmap
              key: audit.service
        - name: DOCUMENT_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: dms-service-urls-configmap
              key: document.service
        - name: COMPLIANCE_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: dms-service-urls-configmap
              key: compliance.service
        - name: LLM_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: dms-service-urls-configmap
              key: llm.service
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: dms-api-gateway-service
  namespace: dms-dev
  labels:
    app: dms-api-gateway-service
    service: api-gateway
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: dms-api-gateway-service
